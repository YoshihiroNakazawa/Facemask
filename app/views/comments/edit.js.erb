$("#<%= @comment.id %>").html("<%= j(render 'comments/edit', { comment: @comment, topic: @comment.topic }) %>").append("<%= j(render 'comments/form', { comment: @comment, topic: @comment.topic }) %>")
